{
  "name": "signal-vault",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "catalog": "node scripts/build-catalog.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "verify": "node scripts/verify.mjs",
    "stage:keepers": "node scripts/stage-keepers.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "music-metadata": "^11.15.0"
  }
}
